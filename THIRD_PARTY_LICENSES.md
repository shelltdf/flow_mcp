# 第三方许可证汇总（运行时）

Vue 应用依赖声明见 **`flow_vue/package.json`**。本文件列出**进入构建产物（`flow_vue/dist/`）**的典型运行时依赖。开发专用工具链见 `devDependencies`。

| 包名 | 许可证 | 说明 |
|------|--------|------|
| [vue](https://www.npmjs.com/package/vue) | MIT | 运行时 UI 框架 |

生成后可使用 `npm ls --prod` 核对生产依赖树；若新增运行时依赖，请更新本表。
