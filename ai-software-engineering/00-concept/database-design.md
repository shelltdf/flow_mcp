# 数据库 / 存储设计

## 范围

本产品初版以**浏览器本地**为主，无独立服务端数据库。

## 本地持久化

| 机制 | 用途 | 说明 |
|------|------|------|
| **localStorage（可选）** | 自动保存草稿、用户偏好（语言、主题） | Key 命名与版本字段见 `02-physical/flowchart-sdi/spec.md`。 |
| **导出文件** | 流程图 JSON 快照 | 程序间形态见 `interface-design.md`。 |

## 演进

若后续接入后端，可在此增加实体与同步策略；须与物理层 `spec.md` 同步。
