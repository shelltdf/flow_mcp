# flowchart-sdi

| 项目 | 值 |
|------|-----|
| **产物** | `dist/` 静态文件（由 Vite 构建） |
| **类型** | 前端 SPA |
| **源码根** | `flow_vue/`：`src/`、`index.html`、`vite.config.ts` |
| **构建** | 在 `flow_vue/` 下 `npm run build` 或 `python build.py`，或仓库根 `python flow_vue/build.py` |
